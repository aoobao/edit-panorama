import { SceneItem } from '@/assets/types'
export interface State {
  sceneList: SceneItem[]
}

const state: State = {
  sceneList: [],
}

const mutations = {}

export default {
  state,
  mutations,
}
